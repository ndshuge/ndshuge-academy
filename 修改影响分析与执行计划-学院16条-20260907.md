# 修改影响分析与执行计划 · 学院 16 条（2026-09-07）

> 来源：鼠哥 2026-09-07 反馈 16+ 条 | 等级：A 级（动引擎结算/进度/UI/门户，按约定先出计划等确认）
> 本次会话已勘察结论（先行调查，非改动）：
> - **权威源 = 四院主 html**（chip/calculus/python/c.html，9-6 22:43 构建提交），design/*3-build 是 9-5 旧存档已落后；引擎改动走 patch_academies.py 同构补丁直打主 html，改完 cp 覆盖 design 预览供验收
> - 四院引擎**完全同构**（events/renderers 行号几乎一致，仅导师名/行号偏移），一处改法可四院套用；CSS 模块（_ui/_chapter/_press/_honor）四院字节相同
> - 主 html 已含 9-6 实现的「我看完了」结算机制（py L1028 只补旧数据/新点亮必经我看完了；L1263 结算按钮文案），但**有缺漏未覆盖全场景**（见 3/9）
> - 门户 index.html 独立维护（today 栏 L661-723、daily_goal L661+、全勤判定 L705-710）

---

## 一、需求归类与执行批次

| 批次 | 内容 | 需求条目 | 性质 |
|---|---|---|---|
| 1 | 结算链路统一 | 3 / 9 / 1 / 10 | 引擎行为（A） |
| 2 | 答题反馈观感 | 14 / 4 / 2 | UI/动效（A，预览后推） |
| 3 | 讲义阅读体验 | 6 / 7 / 12 | UI + 内容规范（A） |
| 4 | Python 专属 | 5（input）+ 8（VSCode 调查） | 调查 + bug（B/A） |
| 5 | 错题重做 | 11 | 功能（A） |
| 6 | 门户 | 15 / 16 | UI（A，预览后推） |
| 7 | 导航 | 13 | 调查后定（A） |

调查项（不阻塞主线，先行）：**8**（C/Python 编辑器 vs VSCode 能力对比）、**13**（右滑返回机制定位）。

---

## 二、逐条分析与修改点

### 批次 1 · 结算链路统一

**需求 3：C 学院结算未走「看参考思路 → 我看完了 → 结算」链路**
- 现状：四院主 html 均已有 soulgo/soulmodel（灵魂拷问自检 + 看参考思路）与 settle-btn（我看完了结算，py L1262-1263）。C 学院疑似存在绕过链路的自动结算路径（code 判题答完即结算？随堂答完即点亮？），需实测各入口。
- 修改点：① 定位 C 学院全部「答完自动结算/点亮」触发点（engine_core L216 结算函数、事件委托里的 settle/auto 分支、code 判题完成回调）；② 统一为：仅当本章「我看完了」被点（或等效的结算闸）才写 chDone + daily_goal；③ 对照芯片/微积分线上行为，以它们为准拉齐。
- 涉及：c.html 主 html（引擎段）+ 对照 chip/calculus.html
- 风险：结算闸收紧后，旧数据/在途章节的「已答完未点亮」状态要兼容（autoHeal 只补旧、不补新，已有 py L1028 模式）

**需求 9：思考题自检完 → 看参考思路 → 我看完了 才结算；取消讲义里「自动结算」文案；有的章缺末尾思考题**
- 现状：py L1028 注释确认「新数据点亮必须经由我看完了按钮」是设计意图，但 L1271/L1395/L1450 等**文案仍写「答完自动结算」**（残留）；开始层 sub L327「答完自动结算点亮章节」同样残留；部分章节 dialogue（灵魂拷问）缺失导致无「看参考思路」按钮 → 结算链断/自动兜底。
- 修改点：① 全 html 搜「自动结算」文案 → 按场景改为「看完讲义与解析，点我看完了结算」口径；② 逐章核对 python（先第七章）与 C 的 dialogue 字段：缺思考题（dialogue 或 dialogue.model）的章补上（内容数据）；③ 结算按钮出现条件 = 题答完 + dialogue 有 model；④ 对齐四院与模板框架。
- 涉及：四院主 html 文案锚点 + python/c 内嵌章节数据
- 风险：文案批量替换注意上下文（卷子「答完自动结算」是正确语义，只改讲义/随堂口径）；补 dialogue 属内容新增，需保证每章 model 文本质量

**需求 1：题目做一半退出，回来要重新做**
- 现状：engine_core/events 已有 sessSnap 会话快照 + 退出守卫（events L360「新会话快照：退出做题页时回滚」、L550-567 回滚实现），设计意图已是「本次作答不留、从头再来」。
- 修改点：① 实测全部退出路径是否都回滚：刷新、左滑返回、Dock 切换、浏览器回退、计时结束、杀进程重开；② 发现漏网路径（如某院未接守卫、某入口未清快照）补齐；③ 确认「重做」时题目顺序/进度确实清零而非续答。
- 涉及：四院主 html（守卫 + 快照 + 恢复三处逻辑）；C 学院若缺守卫要整体接入
- 风险：回滚会影响「继续上次作答」的卷子语义（卷子 st.answered>0 → 继续，events L319）——只对随堂学习做回滚，小卷保持续答，确认口径

**需求 10：已做过的章节回来，不弹「开始做题」层、不显示可再做一遍**
- 现状：开始层 sub 文案「本章已点亮 · 可回来重读重做」（events L327）→ 已做章节也弹开始层并允许重做。
- 修改点：① 已结算（chIsDone）章节：从章节卡点进去直接读讲义（跳过开始层），底部只留回看态（战报常驻），不给「开始做题」；② 重做入口收敛到「按章刷题」模式（isD 刷题模式，答对移出错题）或小卷重做，不占主流程；③ 讲义页内做题入口文案区分「初学」与「复习刷题」。
- 涉及：四院主 html 开始层触发逻辑 + 章节卡 renderers 分支
- 风险：与需求 11（错题重做）入口不冲突；已点亮章的回看不能误触发计时/打卡

### 批次 2 · 答题反馈观感

**需求 14：答错反馈与答对同风格（红色 ❌），删「再试一次」文案，答对答错都别在正确答案上打勾**
- 现状：noteOK/noteNO 双通道（events L116-118/L141-143：toast「✓ 答对 / 看解析，再试一次」）；答错 noteNO 里写「正确答案：…」；答对时选项上可能有 check 图标动画。
- 修改点：① 删 toast「再试一次」类文案；② 答错视觉 = 答对同款动画模板换红色 + ❌（pop/glow 参数继承，色相换红）；③ 移除答题反馈阶段在正确选项上的勾选标记（历史错题回看里的标绿除外，口径单独确认）。
- 涉及：四院主 html 的 noteOK/noteNO + 对应 CSS keyframes + toast 文案
- 风险：先出预览给鼠哥看动画再推（UI 改动必附预览约定）

**需求 4：答对跳出的绿色动画不够丝滑**
- 现状：git log 显示 9-6 已做过一轮 correct fx（soft pop/glow/check-in tick），鼠哥仍不满意。
- 修改点：重做该动画曲线（时长/easing/位移/缩放），参照 apple-ui 动效参数（按压无位移、淡入淡出曲线）；连同需求 14 一起做一套统一反馈语言。
- 涉及：CSS keyframes + 触发点（与 14 同处）
- 风险：同上，预览验收

**需求 2：开始做题弹窗动画抖、底部上滑到顶时下方漏白**
- 现状：开始层 bottom sheet（events L308 起），鼠哥反馈「先上后下」动效抖动激烈；希望弹窗更高，动画到顶时底部不漏。
- 修改点：① 弹窗高度加大（或动画终态使弹窗顶到视口上沿时下缘不出界——改为整卡上移 + 背景渐进）；② easing 换柔和曲线（spring 阻尼调小），避免 overshoot 抖动；③ 参考旧版「先上后下」实现（design 预览/转场报告里有参数）。
- 涉及：开始层 CSS/动画参数（四院 css 模块 SAME 处或引擎内联）
- 风险：纯动效，预览验收后推

### 批次 3 · 讲义阅读体验

**需求 6：讲义重点内容要有旧版黄色框**
- 现状：部分新讲义「重点」以纯文本呈现，旧版有黄色高亮框样式（类 .highlight/yellow-box）未套用。
- 修改点：① 确认讲义渲染是否带重点标记字段（formula/要点句渲染成卡片？）；② 恢复/新增「重点卡」样式（黄底圆角卡），渲染规则对齐旧版观感；③ 内容侧把重点句标出来（若数据层需要加字段则先定义规范，回写手册）。
- 涉及：_chapter.css 或引擎内联样式 + 讲义渲染函数 + 内容数据格式约定
- 风险：样式类名与现有卡片体系冲突检查

**需求 7：讲义代码块默认显示 4 行（有换行再扩），不是一行**
- 现状：代码块初始单行高/横向滚动，阅读费劲。
- 修改点：代码块容器 min-height 默认 ≥4 行（约 4×行高），内容超 4 行自然扩展；CSS 一处改四院生效（_code.css 或内联）。
- 涉及：四院 css + 讲义代码渲染
- 风险：微积分/芯片无代码块（_code.css 是编程学院专属），改动只作用于代码块容器，不误伤

**需求 12：C 学院填空输入框先小后可变大（自适应）**
- 现状：C 填空 input/textarea 初始过大。
- 修改点：填空输入框初始小（1-2 行），聚焦/内容增长自适应扩大（auto-grow textarea）。
- 涉及：c.html 填空渲染 + css（院专属）
- 风险：判题取值逻辑不受输入框尺寸变化影响

### 批次 4 · Python 专属

**需求 5：游乐场跑 input() 报错**
- 现状：pyodide 环境 input() 无交互通道（web 无 stdin）。内容规范 v1 规定判题代码无 input，但游乐场（用户自由输入）会撞上。
- 修改点：运行器拦截 input → 弹浏览器 prompt/自绘输入框（模拟 stdin 一次输入），或友好报错并引导「游乐场暂不支持 input，请用固定变量」。倾向自绘输入框（体验好）。
- 涉及：python.html 代码实验室运行器（pyodide stdin 桥）
- 风险：pyodide 补 stdin 需要运行时钩子，先探测可行性；不可行则降级友好引导

**需求 8：C/Python 编辑器与 VSCode 能力对比（调查项）**
- 交付：能力对照表（语法高亮/补全/报错提示/断点/格式化 等逐项 × 现状），说明「像 VSCode 到什么程度」与做不到的项（浏览器沙箱硬限制），及可改进项排序。
- 不做代码改动（纯调查汇报），鼠哥看后决定是否立项。

### 批次 5 · 错题重做

**需求 11：已订正错题按章节分组，可重新做**
- 现状：错题集 wrong 页（renderers L410+）平铺列表 + 订正入口；订正后移出错题（fixed 页）。
- 修改点：① 错题列表按章分组展示（章头 + 该章错题）；② 增加「整章重做」入口（组级按钮，重做该章全部错题 = 原答错题再出一遍）；③ 与章节刷题模式（答对移出错题）打通。
- 涉及：renderers wrong/fixed 页 + 重做状态机 + 存储键（新增组级进度键要兼容旧数据）
- 风险：进度键新增 → 上线后键管理守则（只增不删）；云同步字段需带新键（回写手册）

### 批次 6 · 门户

**需求 15：today 栏今日全勤也要显示连续全勤天数**
- 现状：index.html L705-710 全勤 → 只显示「🏅 今日全勤」pill，无天数。
- 修改点：读 daily_goal 计算连续全勤天数（连续 N 天四院全点亮），全勤时 pill 显示「🏅 全勤 × N」；非全勤时照旧。
- 涉及：index.html today 渲染段
- 风险：跨 4:00 边界的天数口径与日历一致（读同一数据源）

**需求 16：门户学院已学效果更明显（苹果风格）**
- 现状：L681 每院卡显示「已学 X/Y 章」文本，弱。
- 修改点：按 apple-ui 规范强化已学状态：卡片已完成态（对勾胶囊/进度 X/Y 徽标/绿点/卡片轻高亮），保持「X/Y 文字进度、无百分比条」戒律。
- 涉及：index.html 卡片渲染 + 样式（apple-ui 技能参数）
- 风险：UI 预览确认后推（门户与学院同推或先门户）

### 批次 7 · 导航

**需求 13：学院根页手机右滑回不到总学院**
- 现状：待定位。学院页与门户为同域多页（location 跳转）还是 iframe？右滑返回依赖浏览器历史；若是 iframe 内嵌或 history.replace 则无返回栈。
- 修改点：先查 index.html 进入学院的跳转方式 + 学院 topbar 返回键实现；若右滑不可行（浏览器手势被页面吞），在学院页提供明显的返回入口或手势区域（iOS 边缘右滑模拟）。
- 涉及：定位后定
- 风险：低（先调查）

---

## 三、执行规则（本批次所有改动共同遵守）

1. **源与再生成**：所有引擎改动直接落四院主 html（同构锚点补丁），同步覆盖 design/*-真版-预览.html 供双击预览；不动 design/*3-build 旧存档（除非回归需要）；内容/结构大改前把权威源与存档的差异先对账。
2. **UI 类改动（2/4/6/7/12/14/16）交付前必出可双击预览 HTML**，鼠哥点头才推线上；不口述代替预览。
3. **存储键守则**：不删改现有键（pjc_/pyc_/cc_/chip_/daily_goal/acad_*）；新增键只增不重排。
4. **每批完成后回写《学院工厂手册》引擎内置约定**（结算闸/反馈语言/讲义规范/错题分组新键），手册先行原则反向补齐。
5. **每批完成后本地浏览器回归**（真机/桌面各点一遍主流程），再推 GitHub（4 院 + 门户一个仓库，链接不变）。
6. 修改只限集成站四院 html + 门户 index.html；外部独立站不动（已冻结）。
7. 涉及「我看完了」/结算闸的批次 1 先在小范围（python 某章）真机验证行为，再铺全。

## 四、需要鼠哥拍板的口径

- **Q1 小卷续答**：随堂学习退出 = 从头再来（已定）；小卷是否也退出重做？（现为续答「继续上次作答」）
- **Q2 已做章节重做入口**：主流程不给重做后，复习走「按章刷题」模式即可？（错题会随答对移出）
- **Q3 答错不再标正确答案**：答题当下不显示对错标答后，解析何时展示？（答完一道即展示解析文字保留？）
- **Q4 结算文案口径**：「我看完了！」按钮文案与「答完自动结算」残留文案的替换范围（讲义/随堂改，小卷卷规保留自动结算说法？）

## 五、建议执行顺序

调查项（8/13）可立即并行 → 批次 1（引擎核心，动最多）→ 批次 2（观感，出预览）→ 批次 6（门户）→ 批次 3 → 批次 5 → 批次 4。每批独立验证独立推，不强求一次全量上线。

> 状态：待鼠哥确认「执行」与 Q1-Q4 口径。
